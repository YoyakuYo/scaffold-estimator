feat(superadmin): live presence, unified upload feed, clickable company verify, mandatory company address

Wire end-to-end an ops cockpit for the superadmin and tighten registration:

Backend
- New presence module: user_presence + upload_events tables (migrations 133/134),
  PresenceService (updatePresence, recordAction, recordUpload, getLivePresence,
  getRecentUploadEvents) and admin endpoints
  (GET /admin/presence/live, GET /admin/upload-events).
- Hook recordUpload(...) into drawing, scaffold-config, and vision-bim controllers
  so every upload across products lands in the unified feed.
- CompanyService.getCompanyVerifyDetail joins members + subscriptions + recent
  uploads; exposed as GET /company/admin/:id (superadmin only).
- register.dto.ts: companyName plus structured address (postal code, prefecture,
  city, address line) are now required (@IsNotEmpty).

Frontend
- lib/api/presence.ts client + PagePresenceProvider/usePresence/PresenceTracker:
  mounted in layout-client, heartbeats {pageKey, label} every 30s while logged in.
- usePresence + recordAction sprinkled across dashboard, scaffold list/result,
  quotations list/detail, billing, profile, company, users; Excel export and
  calculate now record actions visible to the cockpit.
- Superadmin dashboard hero replaced with live presence table
  (Name/Email/Company/Page/Label/Last action/Idle) and recent uploads table
  (Time/User/Company/Kind/Filename/Size); companies list now opens
  /superadmin/companies/[id] in a new tab.
- New /superadmin/companies/[id] verification page: full structured address,
  Google Maps verify button, members, subscriptions, and last 20 uploads.
- AddressForm gains requiredFields prop; register page marks postal code,
  prefecture, city, and address line as required and gates submit when empty.
- ja/en/fr translations for all new strings.
